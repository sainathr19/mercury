*.log
*.tsbuildinfo
.DS_Store
.demo-wallet.local
.env
.env.local
.expo-shared/
.expo/
.graph-deploy-key.local
NOTES.local.md
# Anchored to the prebuild output. A bare `android/` matches ANY directory of
# that name at any depth, which silently excluded app/modules/*/android — the
# hand-written native sources, including the Secure Enclave keystore.
/app/android/
build-sim/
build/
contracts/broadcast/
contracts/cache/
contracts/out/
dist/
env.filled
hub/.env
hub/node_modules/
/app/ios/
node_modules/
subgraph/build/
subgraph/generated/
subgraph/node_modules/

# Offchain ENS name registry (issued subnames, per-deployment state)
hub/data/

# The native wallet core is linked in from a separate checkout (see README).
app/vendor/

# Working security list — not for the public repo
SECURITY-FINDINGS.md
